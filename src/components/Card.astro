---
const { class: className = "" } = Astro.props;
---

<div
  class={`rounded-2xl border border-[rgb(var(--border))] bg-[rgb(var(--surface))] p-5 transition duration-200 will-change-transform hover:-translate-y-0.5 ${className}`}
  style="box-shadow: var(--card-shadow);"
>
  <slot />
</div>
